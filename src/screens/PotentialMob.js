import COLORS from "../assets/colors";

const PotentialMob = () => {
    return (
        <div id="potential" style={styles.container}>

        </div>
    );
};

const styles = {
    container: {
        display: "flex",
        alignItems: "center",
    },
};

export default PotentialMob;
